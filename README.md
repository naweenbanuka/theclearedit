# The Clear Edit

Cinematic photography portfolio starter for Cloudflare Pages.

Run locally with `npm install` then `npm run dev`. The current gallery uses temporary Unsplash images; the next phase will replace these with Cloudflare R2-backed uploads.